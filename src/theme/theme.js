import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1976D2", // Blue
    },
    secondary: {
      main: "#9C27B0", // Purple
    },
  },
});

export default theme;
